{
  "name": "twitter-url-rewrite-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "check": "tsc --noEmit",
    "lint": "oxlint --quiet .",
    "lint:fix": "oxlint . --fix",
    "format": "oxfmt --check .",
    "format:write": "oxfmt ."
  },
  "dependencies": {
    "@elysia/openapi": "^1.4.8",
    "elysia": "^1.4.28"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "oxfmt": "^0.18.0",
    "oxlint": "^0.18.0",
    "typescript": "^6.0.3"
  }
}
